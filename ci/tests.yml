---
params:
  CHECK_URL:
platform: linux

image_resource:
  type: docker-image
  source:
    repository: harbor.sandbox.haas.politie:443/platform-team/cf_tools
    tag: latest
    insecure_registries:
    - harbor.sandbox.haas.politie:443

inputs:
- name: resource-git-smoketests

run:
  path: resource-git-smoketests/ci/tests.sh
#  args: [ "https://smoketests.<appdomain>/v1/status" ]

