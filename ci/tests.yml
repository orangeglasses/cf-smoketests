---
params:
  CHECK_URL:
platform: linux

image_resource:
  type: docker-image
  source:
    repository: harbor.cp.haas.politie:443/library/pcf-ops
    tag: latest

inputs:
- name: resource-git-smoketests

run:
  path: resource-git-smoketests/ci/tests.sh
#  args: [ "https://smoketests.<appdomain>/v1/status" ]

